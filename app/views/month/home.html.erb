<% provide(:title, 's This Month') %>

<div class="secondary-header2">
		<%= render 'layouts/navbar' %>
</div>
<div class="container-calendar">
	<div class="container-monthly" style="overflow:hidden;">
		<%= render 'event_calendar/week' %>
		<%= render 'event_calendar/week' %>
		<%= render 'event_calendar/week' %>
		<%= render 'event_calendar/week' %>
		<!--<#%= link_to "Load More", moreweek_path, :remote => true %>-->
	</div>
</div>
